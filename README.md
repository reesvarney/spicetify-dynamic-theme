# Spicetify Dynamic Dark/ Light Theme

This theme adds a selectable dark/ light mode with a button in the top right corner to toggle between the two.

It uses CSS filters to dynamically change the appearance, rather than hard-coded colour overrides.

Most cases should be covered, however if any specific parts of the app do not appear properly, please log an issue.
